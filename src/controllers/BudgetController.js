export const getAllBudgets = (req, res) => {
    try {
        res.json({msg: "Presupuestos"})
    } catch (error) {
        console.log(error)
    }
}

export const getBudgetById = (req, res) => {
    try {
        res.json({msg: "Presupuestos"})
    } catch (error) {
        console.log(error)
    }
}

export const createBudget = (req, res) => {
    try {
        res.json({msg: "Presupuestos"})
    } catch (error) {
        console.log(error)
    }
}

export const updateBudget = (req, res) => {
    try {
        res.json({msg: "Presupuestos"})
    } catch (error) {
        console.log(error)
    }
}

export const deleteBudgetById = (req, res) => {
    try {
        res.json({msg: "Presupuestos"})
    } catch (error) {
        console.log(error)
    }
}